NAVEGAR HASTA CARPETA /ocr
Ejecutar un entorno virtual
python -m venv venv

Activar el entorno virtual
venv\Scripts\activate

Instalar las dependencias
pip install -r requirements.txt

Ejecutar el script de python
python app.py